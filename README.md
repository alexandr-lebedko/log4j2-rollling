# log4j2-rollling

### Config:
* Compresses log file if its size exceeds 10 kb
* Removes compressed files if they are older then 2h
* Removes oldest compressed files if size of all compressed files exceeds 2 kb
